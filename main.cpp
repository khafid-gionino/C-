#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int hasil, a, b, c;
    a = pow(2.5);
    b = 5*pow(2.6);
    c = sqrt(pow(2.5));
    hasil=a+b+c;
    cout<<hasil;
}
